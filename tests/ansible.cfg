[defaults]
# Use local collection for development
collections_path = ../

# Readable output format
stdout_callback = ansible.builtin.default
callback_result_format = yaml

# Reduce output noise
display_skipped_hosts = false
